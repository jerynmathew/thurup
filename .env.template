# Thurup Deployment Configuration Template
# Copy this file to .env.local and customize for your environment

# ========================================
# Port Configuration
# ========================================

# Backend API port (default: 18081)
# NOTE: Must match the proxy target in frontend/vite.config.ts
BACKEND_PORT=18081

# Frontend dev server port (default: 5173)
FRONTEND_PORT=5173

# Reverse proxy port for test mode (default: 8080)
# This proxy serves both frontend and backend through Cloudflare Tunnel
PROXY_PORT=8080

# ========================================
# Backend Environment
# ========================================

# Path to backend .env file (default: ./backend/.env.development)
# Options:
#   - ./backend/.env.development (for dev mode)
#   - ./backend/.env.test (for test mode)
#   - ./backend/.env.production (for production)
BACKEND_ENV=./backend/.env.development

# ========================================
# Cloudflare Tunnel
# ========================================

# Path to cloudflared binary (default: /usr/local/bin/cloudflared)
# Usually auto-installed to this location by brew/package managers
CLOUDFLARED_PATH=/usr/local/bin/cloudflared

# ========================================
# Notes
# ========================================

# This file is for deployment script configuration only.
# Backend-specific settings (database, CORS, logging) are in backend/.env files.
# Frontend settings are in frontend/.env files and vite.config.ts.

# For local development: Use defaults (no need to create .env.local)
# For test mode: Ensure cloudflared is installed
# For production: See DEPLOYMENT.md for full setup guide
